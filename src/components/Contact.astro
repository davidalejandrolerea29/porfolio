---
interface Props {
  email: string;
  github: string;
  linkedin: string;
}

const { email, github, linkedin } = Astro.props;
---
<section id="contact" class="py-20 px-4">
  <h2 class="text-4xl font-bold text-center mb-12 text-gradient">Contact</h2>
  <div class="max-w-md mx-auto flex flex-col gap-6 items-center">
    <a href={`mailto:${email}`} class="flex items-center gap-2 text-white hover:text-purple-400">
      <span>📧</span> {email}
    </a>
    <a href={github} class="flex items-center gap-2 text-white hover:text-purple-400">
      <span>GitHub</span>
    </a>
    <a href={linkedin} class="flex items-center gap-2 text-white hover:text-purple-400">
      <span>LinkedIn</span>
    </a>
  </div>
</section>